do ($ = jQuery) -> $(document).ready(() ->
  socket = io.connect(document.URL.match(/^http:\/\/[^/]*/))
#        $("#button a").click(() ->
 #         open("href", "/urls/admin.html")
  #      )
        #$("#button a").attr("href", "admin.html");
        #$("#button a").attr("href", "user.html");
)